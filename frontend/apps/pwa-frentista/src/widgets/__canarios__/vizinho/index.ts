// Subpasta do MESMO slice do fixture (widgets/__canarios__). Existe para o canário provar o lado
// verde: o boundaries NÃO acusa import dentro do próprio slice (medido em 19/09 — foi assim que
// se descobriu que o "vizinho" precisava morar em widgets/__canario-vizinho__).
export { interno } from './interno';
